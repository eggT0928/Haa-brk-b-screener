# Firebase 전환 검증 기록

검증일: 2026-08-30 (KST). 구현 검증 후 사용자 승인으로 전용 프로젝트에 배포했습니다.

## 전용 Firebase 실제 배포

- 프로젝트: `haa-portfolio-260830` / 표시명 `HAA Portfolio`.
- 웹 주소: <https://haa-portfolio-260830.web.app/>.
- 활성 결제 계정 연결 후 `billingEnabled: true` 확인. 다른 Firebase 프로젝트는 변경하지 않음.
- Firestore Standard `(default)`, `us-central1`, 무료 제공량 적용 DB. 유료 PITR 기능은 비활성.
- Google 로그인 활성, 전용 `firebaseapp.com` / `web.app` 두 도메인 승인 확인.
- `api`, `refresh_daily`, `refresh_intraday` Functions 2세대 배포 완료.
- 운영 제한: 각 함수 최소 인스턴스 0, 최대 1, 동시성 1, 512MiB, CPU 0.3333.
- 자동 작업 2개 활성: 동부시간 평일 장중 15분 간격 및 평일 17:30 일일 갱신.
- 예약 함수 IAM은 호출용 서비스 계정만 허용, 공개 호출 불가. API는 공개 진입점 + 앱 내부 Firebase 인증/승인 검사.
- 7일 초과 배포 컨테이너 이미지 정리 정책 설정. 현재 실행 서비스나 사용자 보유수량을 삭제하는 정책이 아님.
- 실제 Hosting HTTP 200, 미인증 백테스트 API HTTP 401 확인. 브라우저에서 운영 로그인 화면 확인(샘플 모드 아님).
- Python 53개, UI 12개, Firestore 에뮬레이터 보안 규칙 8개를 배포 시 다시 실행해 전부 통과.
- 실제 사용자의 Google 로그인 완료를 확인하고 이메일 검증된 소유자 계정 한 개만 승인. Chrome 실제 화면에서 확정/예상 신호·시장가격 표시 확인. 테스트용 사용자나 임의 보유수량을 운영 DB에 만들지 않음.
- 승인된 실제 Google 로그인 → Hosting → Functions → Firestore 장기 캐시 경로에서 최대기간 백테스트 UI 실행 성공: 2008-07-31 ~ 2026-07-31, 216개월, ^IRX 평균 표시 확인. 보유수량·계산안은 저장하지 않았으며 실제 계좌 데이터 저장/재로그인 복원은 사용자 입력 후 별도 확인 대상.
- 앱 내 브라우저 로그인은 `auth/network-request-failed`가 발생했으나 일반 Chrome에서는 Google 인증·운영 화면 접근을 확인. 앱 내 브라우저 오류가 나면 Chrome에서 위 웹 주소를 직접 여세요.
- 최초 일일 갱신 성공: `2026-08-30T05:42:22.767529Z` (한국 14:42:22). 서버 실행 HTTP 200, 약 10.4초. 후속 중복 요청도 HTTP 200으로 종료.
- 운영 Firestore의 `status/daily.ok=true`, `signals/confirmed` 2026-07, `signalHistory/2026-07`, `signals/preview` 2026-08 및 `internal/history` 저장 확인.
- 주말 초기 예상 신호의 실제 가격 기준시각은 `2026-08-28T20:00:00Z` (한국 8월 29일 05:00 종가). 갱신시각을 실시간 시세 시각으로 오인하면 안 됨.
- 예산 알림·App Check는 아직 설정하지 않음. 최대 인스턴스 제한은 지출의 강제 상한이 아님.

## 로컬 결과

### 2026-08-30 수진 통합 — 운영 반영 대기

- 브랜치 `feat/couple-rebalancer`. 아래는 신규 통합의 로컬 검증이며 위의 기존 HAA 배포 완료 기록과 구분합니다. 통합 Functions·Firestore 규칙·Hosting은 아직 배포하지 않았습니다.
- 원본 수진 저장소 `d80e55d`를 읽기 전용으로 점검했고 원본 저장소·현근 Streamlit·부부 투자 기록 앱은 변경하지 않았습니다.
- Python **86개**, UI **48개**, 실제 Firestore 권한 테스트 **13개** 통과. 타입 검사·프로덕션 빌드·Python 정적 검사 통과.
- HAA 성공 후 수진 실패 시 HAA 확정 신호·성공 상태 보존, 중복/휴장 HAA 실행 시 추가 조회 없음, 공통 시세 재사용으로 추가 다운로드가 QQQM/JEPQ/GLD 3개인지 검증.
- 수진 SPY 고정·SPYM 조정, 초과 보유 차단, 현금 합산, 0.01주/1주 내림, 금액·수량 범위, CSV, 백테스트 고정비중/SPYM/PDBC/^IRX와 실제 월말 누락 거부 검증.
- UI에서 두 사람 탭 전환 시 미저장 입력 유지, 수진 저장·복원 경로 분리, 오래된 시세 확인 후 계산안 추가, 인증 POST, 요청 종료 시 구독·통신 정리 검증.
- 실제 Firestore 에뮬레이터에서 수진/현근 문서 독립성, 미인증·미승인·다른 UID 차단, 모든 이력 행의 형식·범위·NaN/Infinity 거부, 이력 수정·삭제 금지 확인.
- 로컬 Auth→Functions HTTP→Firestore 통합 성공: HAA 401/403/200, 수진 백테스트 401/200, 압축 수진 캐시 복원·fallback. 실제 트랜잭션 잠금의 토큰 검증·동시 실행 차단·실패 후 60초 제한도 확인. 모두 `demo-haa` 가상 데이터이며 운영 계정·보유수량에 쓰지 않음.
- 브라우저 점검 스킬로 320px·390px 모바일 및 1280px 데스크톱 화면을 확인. 페이지 가로 넘침 없음, 모바일 입력 16px 유지, SPY 초과 안내·탭 전환·기간 선택 화면 확인. 임시 화면 크기 설정은 검증 후 복원함. 실제 휴대전화 기기 검증과는 구분함.
- 실제 Yahoo 장기 데이터 읽기 성공: **2022-05-31~2026-07-31**, 기준점 포함 51개 관측, ^IRX 사용 가능, 압축 캐시 약 38KB. 수진 ETF의 공통 이력 때문에 HAA보다 기간이 짧음.
- 같은 로컬 실데이터 점검에서 최신 2026-08-28 종가는 9개 ETF 모두 NaN으로 응답해 2회 시도 후 거부됨. 이는 기존 HAA 로컬 점검에서도 관측됐던 Yahoo 응답 문제이며 검증을 완화하거나 잘못된 시세를 저장하지 않았음. **수진 최신 시세의 운영 서버 정상 수신은 배포 승인 후 별도 확인 대상**.
- 신규 차트 라이브러리·Scheduler·상시 인스턴스 추가 없음. 기존 Firebase 번들의 500KB 초과 경고와 pandas/NumPy 관련 사용 중단 예정 경고는 유지됨.
- GitHub Actions는 새 브랜치에서 계산·UI·빌드·권한 규칙을 검증하도록 구성했습니다. 자동 배포·main 머지는 없습니다.

### 2026-08-30 차트·모바일 후속 개선

- 별도 `feat/haa-interactive-charts` 브랜치. 계산 엔진·데이터베이스·예약 작업은 변경하지 않음.
- UI 테스트 **29개 통과**(기존 12개 + 차트 17개), TypeScript 검사 및 프로덕션 빌드 통과.
- 두 그래프의 마우스 수치 표시, 화면 좌표 환산, 터치 선택 유지, 월별 막대·버튼·키보드 탐색, 기간 변경 초기화, 빈 데이터·단일 관측·낙폭 0 회귀 테스트 추가.
- 실제 브라우저에서 긴 217개 관측 및 짧은 6개 관측의 날짜 눈금, 마우스 툴팁, 키보드 탐색, 접힌 낙폭 차트의 크기 재측정 확인. 이 화면 검증용 값은 가상 데이터이며 운영 신호로 저장하지 않음.
- 320px·390px 모바일 화면 폭에서 그래프 글자 크기 유지, 날짜 자동 축약, 상단 선택 수치, 입력 글자 16px 및 페이지 가로 넘침 없음 확인. 전체 신호/보유수량/백테스트 화면도 320px에서 확인.
- 터치 이벤트는 자동 테스트로 확인했으며 실제 휴대전화 기기에서의 직접 조작 검증과는 구분함.
- 새 차트 라이브러리를 추가하지 않음. 탐색 시 추가 서버 호출 없음. 기존 Firebase 청크의 500KB 초과 빌드 경고는 유지됨.
- `haa-portfolio-260830` Hosting만 재배포 완료. 운영 HTML의 새 JS/CSS 연결 확인. Functions·Firestore 규칙·Scheduler는 재배포하지 않았고 main은 머지하지 않음.
- 기존 Google 로그인 세션으로 운영 최대기간 백테스트(2008-07~2026-07, 217개 관측) 실행 성공. 새 중간 날짜 눈금과 두 그래프의 선택 월 수치·툴팁을 확인. 보유수량·설정·계산안 저장은 실행하지 않음.

### 최초 전환 시 검증

- 원본 9개를 포함한 Python 테스트 **53개 통과**.
- 프런트엔드 수량·컴포넌트 테스트 **12개 통과**.
- 실제 Firestore 에뮬레이터에서 보안 규칙 테스트 **8개 통과**.
- TypeScript 검사 및 Vite 프로덕션 빌드 통과.
- 신규 Python 코드 정적 검사 통과.
- Firebase 예약 함수/API 진입점 import 성공.
- Functions 에뮬레이터가 `api`, `refresh_daily`, `refresh_intraday` 3개 정의를 정상 발견.
- 실제 로컬 Firestore에서 압축 캐시 저장/복원, 트랜잭션 잠금, 중복 실행 억제, 장애 후 직전값 보존 통합 검증 통과.
- 로컬 Auth→Functions HTTP→Firestore 백테스트 통합 검증 통과: 미로그인 401, 미승인 403, 승인 후 200. 운영 Google 로그인 검증과는 별개입니다.
- 샘플 미리보기 HTTP 200 확인. 실제 로그인·외부 배포·실제 주문을 수행한 검증이 아닙니다.
- 프런트엔드 운영 의존성 `npm audit --omit=dev`: 취약점 0건.
- 전체 `npm audit`: Firebase CLI 계열 개발 의존성 moderate 5건. 운영 웹 번들 의존성과 별도이며, 상위 패키지 업데이트 후 재검증이 필요합니다. 강제 메이저 다운그레이드나 임의 override는 적용하지 않았습니다.

## Yahoo 실데이터 점검

현재 기준 조회에서 2026-08-28의 필수 일봉 Close/Adj Close가 모두 NaN으로 응답했습니다. 개별 SPY 조회도 같은 상태였습니다. 재시도 이후에도 해당 응답을 최신 성공값으로 저장하지 않도록 검증이 실패하는 것을 확인했습니다. 이것은 Yahoo의 최신 가격 정상 수신을 검증한 결과가 아닙니다.

별도로 `--as-of 2026-08-28T15:00:00Z`의 **과거 기준시각**을 지정하면 완료된 최근 일봉은 8월 27일입니다. 이 기준으로 실제 Yahoo 데이터 처리를 확인했습니다.

- 완료 월말: 2026-07.
- 백테스트 가능 기간: 2008-07-31 ~ 2026-07-31.
- 수익률 관측 수: 216개월.
- 압축 Firestore 캐시: 약 443KB (900KB 안전 한도 미만, Yahoo 수정주가 재조회에 따라 변동).
- 선택 기간 ^IRX 일별 평균 정상 계산.

이 결과는 과거 데이터 파이프라인 점검이며 오늘의 실전 매매 추천이 아닙니다.

후속 운영 확인: 배포 서버의 첫 일일 실행에서는 8월 28일 데이터가 정상 수신되어 엄격한 기존 검증을 통과했습니다. 로컬에서 보였던 누락값을 우회하거나 과거 기준시각으로 운영 데이터를 만들지 않았습니다. 장중 시세 정상 갱신은 다음 실제 개장 시간에 별도로 확인해야 합니다.

## 추가한 회귀·보호 테스트

- 원본 모멘텀·자산 선택·DBC 프록시 결과 대조.
- 2월/4월/6월/8월 정확한 1·3·6·12개월 월말 참조.
- 예상 TIP 방향 전환이 예상 선택 자산군까지 반영되는지 확인.
- 동일 월말에서 예상·확정 모멘텀 일치.
- TIP 0, 음수, 공격자산 부족·0개, 결측치 거부.
- NYSE 휴일·주말·조기폐장·서머타임·종가 반영 대기시간.
- 현재월 미래 가격/ SPYM 변화가 완료기간 백테스트를 바꾸지 않음.
- 첫 손실 월 MDD, 주말 월말 유지, 기간 범위 제한·중간 누락 월 거부.
- ^IRX 선택 기간 평균·데이터 없음 처리.
- Yahoo 재시도·실패 fallback·타임스탬프 보존·중복 실행 억제·원자적 발행.
- 월별 확정본을 후속 일일 갱신으로 덮어쓰지 않음.
- SPYM 가격 누락 시 SPY 가격 대입 금지, 보유 SPY→SPYM 전환 수량.
- API 미인증/미승인/오류 경로·날짜/캐시 없음 응답.
- Firestore 계정 간 차단, 승인 위조 금지, 신호·가격·캐시 쓰기 금지, 계산안 불변성.

## 운영 확인 목록

- [x] Firebase 프로젝트 ID, 결제 연결, 데이터 위치 확인.
- [ ] 예산 알림 금액 결정·설정.
- [x] 운영용 웹 Firebase 설정 및 Google 로그인 승인 도메인 입력.
- [ ] App Check 적용 여부 결정. 현재 기본은 비활성, 승인 계정 접근만 허용.
- [x] 새 브랜치 GitHub CI 결과 확인 (초기 구현 커밋 CI 성공).
- [x] 별도 프로젝트 배포에 대한 사용자 승인 및 배포.
- [x] Yahoo 최근 완료 일봉 정상 가격과 운영 IP에서의 접근 확인.
- [x] 초기 daily 실행 성공 및 캐시 생성 확인.
- [ ] Google 로그인→관리자 승인→사용자 설정 저장/복원 종단 검증.
- [ ] 미국 장중 Scheduler 예상 갱신 및 실제 월말 확정 저장 검증.
- [ ] 기존 실계좌 가격과 수량 계산 대조. 자동 주문은 없음.
- [ ] 최종 main 머지 승인 (배포 승인과 별개).
